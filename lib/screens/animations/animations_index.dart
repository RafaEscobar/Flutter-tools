import 'package:flutter/material.dart';

class AnimationsIndex extends StatelessWidget {
  const AnimationsIndex({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}