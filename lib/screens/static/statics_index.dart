import 'package:flutter/material.dart';
class StaticsIndex extends StatelessWidget {
  const StaticsIndex({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}